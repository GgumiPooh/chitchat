.git
.github
.claude
.vscode
.idea

node_modules
.next
out
build
dist

.env
.env.*
!.env.example

docs
downloads
drizzle
scripts
# WARN: REQUIREMENTS.md § 16. `pnpm build` runs these three, so they are part of the image build rather than tooling beside it — excluded with the rest of `scripts`, the builder stage fails at `ERR_MODULE_NOT_FOUND` where a local build passes on a file it never had to copy. The ops entries stay out: they run on a GitHub runner and nothing in the image calls them.
!scripts/generate-ffmpeg-core.ts
!scripts/generate-cutout-model.ts
!scripts/generate-offline-precache.ts
*.md

.DS_Store
*.log
npm-debug.log*
tsconfig.tsbuildinfo
coverage
.turbo

docker-compose.yml
infra
Dockerfile
.dockerignore
