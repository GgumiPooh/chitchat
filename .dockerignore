.git
.github
.claude
.vscode
.idea

node_modules
.next
out
build
dist

.env
.env.*
!.env.example

docs
downloads
drizzle
scripts
# WARN: REQUIREMENTS.md § 16. `pnpm build` ends in `precache`, so this one script is part of the image build rather than tooling beside it — excluded with the rest of `scripts`, the builder stage fails at `ERR_MODULE_NOT_FOUND` where a local build passes on a file it never had to copy. The ops entries stay out: they run on a GitHub runner and nothing in the image calls them.
!scripts/generate-offline-precache.ts
*.md

.DS_Store
*.log
npm-debug.log*
tsconfig.tsbuildinfo
coverage
.turbo

docker-compose.yml
infra
Dockerfile
.dockerignore
